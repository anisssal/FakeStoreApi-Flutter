class RoutePath {
  const RoutePath._();

  static const home = '/';
  static const login = '/login';
  static const products = '/products';
  static const productDetail = '/products/detail';
  static const cart = '/cart';
}

enum RouteName{ home, login, products, productDetail, cart}